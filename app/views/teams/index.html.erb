<% provide(:title,"Teams") %>
<h1>Teams list</h1>
<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <td>Sl No.</td>
      <td>Team</td>
      <td class="hidden-phone hidden-mobile">Description</td>
      <td>Members</td>
    <tr>
  <thead>
  <tbody>
  	<% counter=1 %>
    <% @teams.each do |team| %>
    
    <tr>
      <td><%= counter %></td>
      <td><%= link_to team.name, team_path(team)  %></td>
      <td class="hidden-phone hidden-mobile"><%= team.description %></td>
      <td>
      	<% userlist=[]  
      	   team.users.each do |user|
      	     userlist << user.name
      	  %>
      	 <%= link_to user.name , user_path(user) %> 
      	 ,  
      	 <%  end  %>
     </td>
    </tr>
    </a>
    <% counter+=1 %>
   <% end %>
  </tbody>
</table>



